# apps/

Application surfaces. Scaffolded by `/ideator` + FOUNDRY from `doc/ARCHITECTURE.md`.

- `desktop/` — the local-first **Tauri** app: Rust shell + React/TS frontend, SQLite + SQLCipher.
  Hosts onboarding, the job inbox, the match/coverage review UI, document preview, and export.

This directory is a placeholder until the build phase begins.
